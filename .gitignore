node_modules
dist
.env
.env.local
.env.*.local
*.local
.DS_Store
supabase/.branches
supabase/.temp

# tsc -b incremental caches. Machine-local, regenerated every build, and a
# guaranteed merge conflict for anyone working on the same branch.
*.tsbuildinfo
